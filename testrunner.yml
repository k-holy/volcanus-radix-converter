general:
  framework: phpunit
  test_targets:
    recursive: true
    resources:
      - Tests
  autotest:
    enabled: false
    watch_dirs:
      - Tests
  notify: true

phpunit:
  config: phpunit.xml
